# ============================================================================
# Isolated Docker Runner - Configuración
# ============================================================================
# Copia este archivo a config/.env para personalizar tu instalación:
#
#   cp config/.env.example config/.env
#
# Luego edita config/.env con tus valores preferidos.
# ============================================================================

# ============================================================================
# Configuración común (Ubuntu y macOS)
# ============================================================================

# Directorio donde se almacenan los datos de Docker (solo Ubuntu)
# Por defecto: $HOME/.isodocker
ISO_DOCKER_HOME="$HOME/.isodocker"

# Nombre del perfil de Colima (solo macOS)
# Útil si quieres tener múltiples instalaciones aisladas
COLIMA_PROFILE="isodocker"

# ============================================================================
# Configuración de recursos para Colima (solo macOS)
# ============================================================================

# Número de CPUs asignadas a Colima
# Recomendado: 2 para uso normal, 4 para proyectos grandes
COLIMA_CPU=2

# Memoria RAM en GB asignada a Colima
# Recomendado: 4 GB para uso normal, 8 GB para proyectos grandes
COLIMA_MEMORY=4

# Espacio en disco en GB asignado a Colima
# Recomendado: 30 GB para uso normal, 60 GB si usas muchas imágenes
COLIMA_DISK=30